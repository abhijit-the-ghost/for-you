import LoveTimeline from "../components/LoveTimeline"; // adjust path as needed

const LoveTimelinePage = () => {
  return (
    <div className="min-h-screen bg-base-100">
      {/* You can add a Back button here if using react-router */}
      <LoveTimeline />
    </div>
  );
};

export default LoveTimelinePage;
